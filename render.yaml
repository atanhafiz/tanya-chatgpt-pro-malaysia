services:
  - type: web
    name: tanya-chatgpt-pro-malaysia
    env: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: npm start
    rootDir: .
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000
